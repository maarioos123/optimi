from VRP_Model import Model

m = Model()
m.BuildModel()

# test.txt file import
# for i in m.allNodes:
#     print(str(i.ID) + ", " +str(i.x) + ", " +str(i.y) + ", " +str(i.demand) + ", " +str(i.untime))
